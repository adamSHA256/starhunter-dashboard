# Dev: hit the API directly (not needed in production behind nginx proxy)
VITE_API_URL=https://release-current.starhunter.software/Api/graphql
VITE_API_TOKEN=your-token-here

# Dev: base URL for avatar images (not needed in production — nginx proxies /_uploads/)
VITE_ASSET_BASE_URL=https://release-current.starhunter.software
